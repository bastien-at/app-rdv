apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: {{ template "fullname" . }}
spec:
  source:
    volumeSnapshotContentName: {{ template "fullname" . }}-{{ .Values.namespace }}
