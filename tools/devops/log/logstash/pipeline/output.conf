output {
  elasticsearch {
    hosts => "https://logs.alltricks.sexy:9200"
    user => "logstash_internal"
    password => "${LOGSTASH_INTERNAL_PASSWORD}"
    data_stream => "true"
    data_stream_type => "logs"
    data_stream_dataset => "logstash"
    data_stream_namespace => "at_prod"
  }
}
