input {
  jdbc {
    jdbc_driver_class => "com.microsoft.sqlserver.jdbc.SQLServerDriver"
    jdbc_connection_string => "jdbc:sqlserver://;serverName=10.11.0.8;databaseName=AVANIS_SQL_SNAPSHOT;encrypt=false;trustServerCertificate=true"
    jdbc_user => "${PIPELINE_AT_PROD_MSSQL_USER:-}"
    jdbc_password => "${PIPELINE_AT_PROD_MSSQL_PASS:-}"
    schedule => "0 * * * * *"
    statement => "SELECT COUNT(*) AS documents  FROM [AVANIS_SQL_SNAPSHOT].dbo.F_DOCENTETE WHERE DO_Type= 6 and DO_Valide = 0"
    add_field => {
      "input.name" => "mssql_sage_invoice_status_checker_to_validate"
    }
  }
  jdbc {
    jdbc_driver_class => "com.microsoft.sqlserver.jdbc.SQLServerDriver"
    jdbc_connection_string => "jdbc:sqlserver://;serverName=10.11.0.8;databaseName=AVANIS_SQL_SNAPSHOT;encrypt=false;trustServerCertificate=true"
    jdbc_user => "${PIPELINE_AT_PROD_MSSQL_USER:-}"
    jdbc_password => "${PIPELINE_AT_PROD_MSSQL_PASS:-}"
    schedule => "0 * * * * *"
    statement => "SELECT COUNT(*) AS documents  FROM [AVANIS_SQL_SNAPSHOT].dbo.F_DOCENTETE WHERE DO_Type= 6 and DO_Valide = 1"
    add_field => {
      "input.name" => "mssql_sage_invoice_status_checker_to_account"
    }
  }
}
